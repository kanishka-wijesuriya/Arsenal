<UserControl x:Class="Arsenal.UI.Views.Pages.PerformancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:controls="clr-namespace:Arsenal.UI.Controls"
             xmlns:vm="clr-namespace:Arsenal.UI.ViewModels"
>
    <Grid>
    <ScrollViewer x:Name="PageScroller" Style="{StaticResource PageScrollStyle}">
        <StackPanel Style="{StaticResource PageBodyStyle}">

            <Grid x:Name="PageHeader" Style="{StaticResource PageHeaderStyle}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Margin="0,0,24,0">
                    <TextBlock Text="Performance" Style="{StaticResource PageTitleStyle}"/>
                    <TextBlock Text="Profiles, power limits, undervolting, GPU tuning and fan curves" Style="{StaticResource PageSubtitleStyle}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Style="{StaticResource ActionBarStyle}">
                    <ui:Button Style="{StaticResource ActionButtonStyle}" Content="New plan" Command="{Binding OpenCreateProfileCommand}" Appearance="Secondary" Icon="{ui:SymbolIcon Add24}"/>
                    <ui:Button Style="{StaticResource ActionButtonStyle}" Content="Reset profile" Command="{Binding ResetSettingsCommand}" Appearance="Secondary" Icon="{ui:SymbolIcon ArrowReset24}"/>
                    <ui:Button Style="{StaticResource ActionButtonStyle}" Margin="0" Content="Apply changes" Command="{Binding ApplySettingsCommand}" Appearance="Primary" Icon="{ui:SymbolIcon Checkmark24}"/>
                </StackPanel>
            </Grid>

            <!-- Pinned: every other group on this page tunes whatever profile is chosen
                 here, so the choice has to be visible while the page is being read. In a
                 theme that puts settings one level down this group stays on the list
                 instead of becoming a destination; in the others it changes nothing. -->
            <controls:SettingsGroup x:Name="ProfileGroup" Header="Active profile"
                                    AlwaysOpen="True"
                                    Icon="Gauge24"
                                    Description="The tuning below belongs to whichever profile is selected here. Switching profiles loads its own saved values.">
                <controls:SettingsRow Header="Profile"
                                      Description="Applied to the BIOS and the Windows power mode.">
                    <StackPanel>
                        <ItemsControl ItemsSource="{Binding VisibleProfiles}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Style="{StaticResource RowChoiceGroupStyle}"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <RadioButton Style="{StaticResource RowSegmentStyle}"
                                                 GroupName="PerformanceProfile"
                                                 Content="{Binding Name}"
                                                 IsChecked="{Binding IsSelected, Mode=OneWay}"
                                                 Command="{Binding DataContext.SelectProfileModeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                 CommandParameter="{Binding ModeIndex}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ui:Button Content="View all" Command="{Binding OpenProfileManagerCommand}"
                                   Appearance="Transparent" HorizontalAlignment="Left" Margin="-8,6,0,-4"
                                   Icon="{ui:SymbolIcon AppsListDetail24}"
                                   Visibility="{Binding HasMoreProfiles, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </controls:SettingsRow>
            </controls:SettingsGroup>

            <controls:SettingsGroup Header="CPU power and thermals" Icon="DeveloperBoard24"
                                    Description="Power limits, the temperature ceiling and undervolting, saved with the selected profile.">
                <controls:SettingsRow Header="Apply custom power limits"
                                      Description="Write the values below to the CPU whenever this profile is applied.">
                    <ui:ToggleSwitch IsChecked="{Binding ApplyPower}"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="CPU boost policy"
                                      Description="Windows processor performance boost mode for this profile.">
                    <ComboBox SelectedIndex="{Binding CpuBoost}" MinWidth="240">
                        <ComboBoxItem Content="Disabled"/>
                        <ComboBoxItem Content="Enabled"/>
                        <ComboBoxItem Content="Aggressive"/>
                        <ComboBoxItem Content="Efficient enabled"/>
                        <ComboBoxItem Content="Efficient aggressive"/>
                        <ComboBoxItem Content="Aggressive at guaranteed"/>
                        <ComboBoxItem Content="Efficient at guaranteed"/>
                    </ComboBox>
                </controls:SettingsRow>

                <!-- Every range below comes from the view model rather than from a number
                     typed here: the writers reject anything outside the machine's own
                     bounds, and those bounds differ per chassis and per CPU. Minimum and
                     Maximum are written before Value on each slider so the track is sized
                     before the value lands on it. -->
                <controls:SettingsRow Header="Sustained power" Description="SPL / PL1: the limit the CPU can hold indefinitely.">
                    <controls:ValueSlider Minimum="{Binding PowerLimitRange.Minimum}" Maximum="{Binding PowerLimitRange.Maximum}"
                                          Value="{Binding Spl}" Format="{}{0} W"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Slow burst" Description="SPPT / PL2: short bursts above the sustained limit.">
                    <controls:ValueSlider Minimum="{Binding PowerLimitRange.Minimum}" Maximum="{Binding PowerLimitRange.Maximum}"
                                          Value="{Binding Sppt}" Format="{}{0} W"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Fast peak" Description="FPPT: the momentary ceiling for spiky workloads.">
                    <controls:ValueSlider Minimum="{Binding PowerLimitRange.Minimum}" Maximum="{Binding PowerLimitRange.Maximum}"
                                          Value="{Binding Fppt}" Format="{}{0} W"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Temperature limit" Description="The CPU throttles once it reaches this temperature.">
                    <controls:ValueSlider Minimum="{Binding CpuTempRange.Minimum}" Maximum="{Binding CpuTempRange.Maximum}"
                                          Value="{Binding CpuTempLimit}" Format="{}{0}°C"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="CPU undervolt" Description="Curve Optimizer offset. Too aggressive a value can destabilise the system."
                                      Visibility="{Binding IsUndervoltSupported, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <controls:ValueSlider Minimum="{Binding CpuUndervoltRange.Minimum}" Maximum="{Binding CpuUndervoltRange.Maximum}"
                                          Value="{Binding CpuUndervolt}" Format="{}{0} mV"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="iGPU undervolt" Description="Voltage offset for the integrated graphics."
                                      Visibility="{Binding IsIgpuUndervoltSupported, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <controls:ValueSlider Minimum="{Binding IgpuUndervoltRange.Minimum}" Maximum="{Binding IgpuUndervoltRange.Maximum}"
                                          Value="{Binding IgpuUndervolt}" Format="{}{0} mV"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Apply undervolt with this profile"
                                      Description="Leave off to tune the offsets without writing them yet."
                                      Visibility="{Binding IsUndervoltRowVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <ui:ToggleSwitch IsChecked="{Binding ApplyUndervolt}"/>
                </controls:SettingsRow>
            </controls:SettingsGroup>

            <controls:SettingsGroup Header="Dedicated GPU" Icon="Glance24"
                                    Description="Clock offsets and power targets applied to the discrete GPU for this profile."
                                    Visibility="{Binding IsDedicatedGpuPresent, Converter={StaticResource BooleanToVisibilityConverter}}">
                <controls:SettingsRow Header="Core clock offset">
                    <controls:ValueSlider Minimum="{Binding GpuCoreOffsetRange.Minimum}" Maximum="{Binding GpuCoreOffsetRange.Maximum}"
                                          Value="{Binding GpuCoreOffset}" Format="{}{0} MHz"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Memory clock offset">
                    <controls:ValueSlider Minimum="{Binding GpuMemoryOffsetRange.Minimum}" Maximum="{Binding GpuMemoryOffsetRange.Maximum}"
                                          Value="{Binding GpuMemoryOffset}" Format="{}{0} MHz"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Dynamic boost" Description="Extra wattage the GPU may borrow from the CPU budget.">
                    <controls:ValueSlider Minimum="{Binding GpuBoostRange.Minimum}" Maximum="{Binding GpuBoostRange.Maximum}"
                                          Value="{Binding GpuBoost}" Format="{}{0} W"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Temperature target">
                    <controls:ValueSlider Minimum="{Binding GpuTempRange.Minimum}" Maximum="{Binding GpuTempRange.Maximum}"
                                          Value="{Binding GpuTempTarget}" Format="{}{0}°C"/>
                </controls:SettingsRow>

                <!-- Shown as the whole TGP, not as the offset the register takes; the view
                     model shifts it back. Hidden where the firmware reports no base to
                     offset, which is also what the ceiling is derived from. -->
                <controls:SettingsRow Header="Power target"
                                      Visibility="{Binding IsGpuPowerAdjustable, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <controls:ValueSlider Minimum="{Binding GpuPowerTotalRange.Minimum}" Maximum="{Binding GpuPowerTotalRange.Maximum}"
                                          Value="{Binding GpuPowerTotal}" Format="{}{0} W"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Maximum GPU clock">
                    <controls:ValueSlider Minimum="{Binding GpuClockLimitRange.Minimum}" Maximum="{Binding GpuClockLimitRange.Maximum}"
                                          TickFrequency="50" Value="{Binding GpuClockLimit}" ValueText="{Binding GpuClockLimitText}"/>
                </controls:SettingsRow>
            </controls:SettingsGroup>

            <controls:SettingsGroup Header="Fan curves" Icon="WeatherDuststorm24"
                                    Description="Drag the eight control points to set fan speed against temperature. Each profile keeps its own curves."
                                    Visibility="{Binding IsCustomFansSupported, Converter={StaticResource BooleanToVisibilityConverter}}">
                <controls:SettingsRow Header="Apply custom fan curves with this profile">
                    <ui:ToggleSwitch IsChecked="{Binding ApplyFans}"/>
                </controls:SettingsRow>

                <controls:SettingsRow>
                    <controls:SettingsRow.ContentBelow>
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Curve editor" Style="{StaticResource SettingLabelStyle}" HorizontalAlignment="Left"/>
                                <ui:Button Grid.Column="1" Content="Calibrate max RPM" Command="{Binding StartFanCalibrationCommand}" Appearance="Secondary" Icon="{ui:SymbolIcon Play24}"/>
                            </Grid>

                            <!-- The same segmented control the profile row above uses,
                                 rather than a TabControl. The stock tab strip kept its own
                                 chrome around a restyled TabItem, which clipped the
                                 selected pill's corners and left it sitting on a band that
                                 belonged to neither the page nor the card. One canvas is
                                 bound to whichever curve is chosen, so switching fans no
                                 longer builds three editors either. -->
                            <WrapPanel Style="{StaticResource ChoiceGroupStyle}" Margin="0,0,0,12">
                                <RadioButton Style="{StaticResource SegmentedRadioButtonStyle}" GroupName="FanCurve"
                                             Content="CPU fan"
                                             IsChecked="{Binding SelectedFanTab, Mode=OneWay, Converter={StaticResource IntEqualsConverter}, ConverterParameter=0}"
                                             Command="{Binding SelectFanTabCommand}" CommandParameter="0"/>
                                <RadioButton Style="{StaticResource SegmentedRadioButtonStyle}" GroupName="FanCurve"
                                             Content="GPU fan"
                                             IsChecked="{Binding SelectedFanTab, Mode=OneWay, Converter={StaticResource IntEqualsConverter}, ConverterParameter=1}"
                                             Command="{Binding SelectFanTabCommand}" CommandParameter="1"/>
                                <!-- Only on chassis that have a third fan. -->
                                <RadioButton Style="{StaticResource SegmentedRadioButtonStyle}" GroupName="FanCurve"
                                             Content="Mid fan"
                                             Visibility="{Binding HasMidFan, Converter={StaticResource BooleanToVisibilityConverter}}"
                                             IsChecked="{Binding SelectedFanTab, Mode=OneWay, Converter={StaticResource IntEqualsConverter}, ConverterParameter=2}"
                                             Command="{Binding SelectFanTabCommand}" CommandParameter="2"/>
                            </WrapPanel>

                            <Border Style="{StaticResource InsetSurfaceStyle}" Height="230">
                                <controls:FanCurveCanvas CurveModel="{Binding SelectedFanCurve}" Margin="10"/>
                            </Border>
                        </StackPanel>
                    </controls:SettingsRow.ContentBelow>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Ramp-up hysteresis" Description="How far the temperature must rise before the fan speeds up.">
                    <controls:ValueSlider Minimum="{Binding FanHysteresisRange.Minimum}" Maximum="{Binding FanHysteresisRange.Maximum}"
                                          Value="{Binding FanHysteresisUp}" Format="{}{0}°"/>
                </controls:SettingsRow>

                <controls:SettingsRow Header="Ramp-down hysteresis" Description="How far it must fall again before the fan slows down.">
                    <controls:ValueSlider Minimum="{Binding FanHysteresisRange.Minimum}" Maximum="{Binding FanHysteresisRange.Maximum}"
                                          Value="{Binding FanHysteresisDown}" Format="{}{0}°"/>
                </controls:SettingsRow>
            </controls:SettingsGroup>

        </StackPanel>
    </ScrollViewer>

        <!-- The library uses the same in-window overlay language as Setup: a focused
             surface, proper scrim, and no detached system dialog. -->
        <controls:OverlayHost x:Name="ProfileManagerOverlay" Panel.ZIndex="8" PanelMaxWidth="760" PanelMaxHeight="620"
                              IsLightDismissEnabled="True"
                              IsOpen="{Binding IsProfileManagerOpen, Mode=TwoWay}">
            <Grid Width="700" MaxHeight="560" Margin="26">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="Performance plan library" FontSize="22" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimary}"/>
                        <TextBlock Text="Choose, name, and organize every built-in and custom plan in one place." Margin="0,5,0,0"
                                   FontSize="12.5" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}"/>
                    </StackPanel>
                    <ui:Button Grid.Column="1" Icon="{ui:SymbolIcon Dismiss24}" Appearance="Secondary"
                               Width="34" Height="34" Padding="0" Command="{Binding CloseProfileManagerCommand}"/>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource InsetSurfaceStyle}" Margin="0,20,0,14" Padding="16"
                        Visibility="{Binding IsCreateComposerOpen, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,14,0">
                            <TextBlock Text="Start from the current plan" Style="{StaticResource SettingLabelStyle}"/>
                            <TextBlock Text="Power, GPU, and fan settings are copied so you can refine them without rebuilding the profile." Margin="0,3,0,10"
                                       FontSize="11.5" TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}"/>
                            <TextBox Text="{Binding NewProfileName, UpdateSourceTrigger=PropertyChanged}" MaxLength="48"
                                     MinWidth="300" AutomationProperties.Name="Plan name"/>
                        </StackPanel>
                        <ui:Button Grid.Column="1" Content="Create plan" Command="{Binding CreateProfileCommand}"
                                   Appearance="Primary" Icon="{ui:SymbolIcon Add24}" VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="2" Style="{StaticResource PageScrollStyle}" MaxHeight="360">
                    <ItemsControl ItemsSource="{Binding Profiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource SurfaceCard}" BorderBrush="{DynamicResource StrokeSubtle}"
                                        BorderThickness="1" CornerRadius="{DynamicResource RadiusCard}" Padding="14" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="28" Height="28" CornerRadius="8" Background="{DynamicResource AccentSubtle}"
                                                VerticalAlignment="Center">
                                            <ui:SymbolIcon Symbol="Gauge24" FontSize="15" Foreground="{DynamicResource AccentPrimary}"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="10,0,16,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextPrimary}"
                                                       Visibility="{Binding IsCustom, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="48" MinWidth="230"
                                                     Visibility="{Binding IsCustom, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <TextBlock FontSize="10.5" Margin="0,3,0,0" Foreground="{DynamicResource TextSecondary}">
                                                <Run Text="Profile"/><Run Text="  ·  "/><Run Text="{Binding ModeIndex, Mode=OneWay}"/>
                                            </TextBlock>
                                            <TextBlock Text="Delete this plan and its saved tuning?" Margin="0,7,0,0"
                                                       Foreground="{DynamicResource TextSecondary}"
                                                       Visibility="{Binding IsDeletePending, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                            <ui:Button Content="Select plan" Appearance="Secondary"
                                                       Command="{Binding DataContext.SelectManagedProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}" Margin="0,0,6,0"/>
                                            <ui:Button Icon="{ui:SymbolIcon Checkmark24}" Appearance="Secondary" Width="34" Padding="0"
                                                       ToolTip="Save name"
                                                       Command="{Binding DataContext.RenameProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Visibility="{Binding IsDeleteIdle, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <ui:Button Icon="{ui:SymbolIcon Delete24}" Appearance="Secondary" Width="34" Padding="0" Margin="6,0,0,0"
                                                       ToolTip="Delete plan"
                                                       Command="{Binding DataContext.RequestDeleteProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Visibility="{Binding IsDeleteIdle, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <ui:Button Content="Keep plan" Appearance="Secondary" Margin="6,0,0,0"
                                                       Command="{Binding DataContext.CancelDeleteProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       Visibility="{Binding IsDeletePending, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <ui:Button Content="Delete plan" Appearance="Primary" Margin="6,0,0,0"
                                                       Command="{Binding DataContext.ConfirmDeleteProfileCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                       CommandParameter="{Binding}"
                                                       Visibility="{Binding IsDeletePending, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Grid Grid.Row="3" Margin="0,14,0,0">
                    <TextBlock Text="{Binding ProfileManagerMessage}" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondary}"/>
                    <ui:Button Content="New plan" Command="{Binding ShowCreateComposerCommand}"
                               Appearance="Secondary" Icon="{ui:SymbolIcon Add24}" HorizontalAlignment="Right"
                               IsEnabled="{Binding CanCreateProfile}"/>
                </Grid>
            </Grid>
        </controls:OverlayHost>
    </Grid>
</UserControl>
